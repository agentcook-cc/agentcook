spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/agentcook_business
    username: agentcook
    password: agentcook_dev

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    show-sql: true

logging:
  level:
    cc.agentcook: DEBUG
    org.hibernate.SQL: DEBUG
