/**
 * AgentCook Infrastructure Layer.
 *
 * Contains JPA Repository implementations, Redis cache adapters,
 * Flyway migrations, and external service integrations (gRPC client to Python).
 */
package cc.agentcook.infrastructure;
