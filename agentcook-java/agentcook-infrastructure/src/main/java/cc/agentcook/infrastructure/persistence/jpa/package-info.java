/**
 * Spring Data JPA Repository declarations (raw {@code JpaRepository<Entity, UUID>}).
 * These are persistence-level interfaces — domain code should not depend on them.
 * Adapters in {@code ..adapter} translate them into the Domain Repository ports.
 */
package cc.agentcook.infrastructure.persistence.jpa;
