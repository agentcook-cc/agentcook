/**
 * Persistence layer - JPA Repository implementations for domain aggregates.
 *
 * Implements domain repository interfaces using Spring Data JPA.
 * Each aggregate has a corresponding JPA entity + repository impl.
 */
package cc.agentcook.infrastructure.persistence;
