# LangGraph Multi-Agent POC

agentcook 的声明式 multi-agent 路由 POC。

## 核心差异化
- **LangGraph**：命令式（写 Python 定义状态机）
- **agentcook**：声明式（YAML 配置，运行时编译为 LangGraph）

## 运行
```bash
pip install -r requirements.txt
python demo_multi_agent.py
```
